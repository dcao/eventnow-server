table! {
    groups (id) {
        id -> Int4,
        name -> Varchar,
        description -> Nullable<Varchar>,
        profile_img -> Nullable<Varchar>,
        cover_img -> Nullable<Varchar>,
    }
}
